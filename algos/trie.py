class Node:

	def __init__(self, count, children=[None]*26):
		self.count = count
		self.children = children

	@classmethod
	def insert(cls, node, keys):


if __name__ == '__main__':
	keys = ["the", "a", "there", "answer", "any", "by", "bye", "their"]